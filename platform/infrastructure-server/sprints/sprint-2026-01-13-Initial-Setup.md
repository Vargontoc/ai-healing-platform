# Sprint Review: [Configuration Management]
## Objetivos
- Crear archivo `application.yml` global en `config-repo`
- Crear archivo `gateway-server.yml` en `config-repo`
- Crear archivo `agent-orchestrator-service.yml` en `config-repo`
- Crear archivo `incident-manager-service.yml` en `config-repo`
- Verificar que Infrastructure Server sirve estas configuraciones

## Criterios de Aceptación
1. Curl a `http://localhost:8761/gateway-server/default` devuelve el JSON correcto.

## Estado del Agente
- **Última ejecución de tests:**
- **Errores encontrados:** 

## Propuesta de Siguientes Pasos